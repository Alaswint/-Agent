# ===== Roleplay Agent 环境变量示例 =====
# 复制为 .env 使用（需自行加载，或用系统环境变量设置）

# 项目根目录（生产模式下 Next.js 的 cwd 可能不同，用它保证读写 data/ 等目录的位置正确）
# 不设置时会自动向上查找包含 package.json 的目录
# PROJECT_ROOT=C:\path\to\roleplay-agent

# 解除命令白名单限制：允许执行任意命令（危险命令黑名单仍然生效）
# 默认关闭，仅允许 node/npm/git/python 等白名单命令
# ROLEPLAY_ALLOW_ANY_COMMAND=0
